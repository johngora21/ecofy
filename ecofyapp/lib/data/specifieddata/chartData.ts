export interface ChartConfig {
  gridLines: number[];
  chartDimensions: {
    width: number;
    height: number;
    padding: number;
  };
}

export const chartConfig: ChartConfig = {
  gridLines: [0, 1, 2, 3, 4],
  chartDimensions: {
    width: 660,
    height: 260,
    padding: 40
  }
}; 
 
  gridLines: number[];
  chartDimensions: {
    width: number;
    height: number;
    padding: number;
  };
}

export const chartConfig: ChartConfig = {
  gridLines: [0, 1, 2, 3, 4],
  chartDimensions: {
    width: 660,
    height: 260,
    padding: 40
  }
}; 
 